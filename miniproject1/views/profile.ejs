<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="min-h-screen w-full bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-10 bg-red-500 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover " src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-3xl"><span class="font-light">Hello, </span> <%= user.name %></h3>
        </div>
        <h5 >you can create a new post</h5>
        <form class="mt-5" method="post" action="/post">
            <textarea name="content" placeholder="What's on your mind?" class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md block"></textarea>
            <input class="px-3 py-2 bg-blue-500 block rounded-md mt-5 " type="submit" value="Create Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your posts</h3>
            <div class="postContainer mt-5">
                <%  user.posts.forEach(function(post){%>
                    <div class="post w-1/3 p-4 rounded-md mb-4 border-[1px] bg-zinc-800 border-zinc-700">
                        <h4 class="text-blue-500 mb-2"><%=user.username%></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <small class="inline-block mt-2"><%= post.likes.length %> likes</small>
                        <div class="btns flex gap-5 mt-1">
                            <a class="text-blue-500" href="/like/<%=post._id%>">
                                <%= post.likes.indexOf(user._id) === -1? "Like":"Unlike" %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%= post._id %>">edit</a>
                        </div>
                    </div>
                <%})%>
            </div>
        </div>
    </div>
</body>
</html>